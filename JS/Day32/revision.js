/*
    1. async - It is a keyowrd which is used to create a async function inside the js, which always returns 
       a promise.

    2. await - It is a keyword which is used inside the async function during an api call. It pauses the 
       execution of async function until a promise is resolved.

    3. Major difference between async & await and promises is that, async & await pauses the execution 
       inside the async function until the promise is resolved. 

       Furthermore, await returns a resolved or rejected value, it doesn't returns a promise object


*/