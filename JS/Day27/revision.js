/**
    - Event loop - It is a monitor which is continously monitoring the callstack and the callback queue
      whenever a callstack is empty it puts the callback function inside the callstack from the callback
      queue.    

      Event loop is a mechanism which allows async operations inside the JS by monitoring the call stack 
      and callback queue.

 */