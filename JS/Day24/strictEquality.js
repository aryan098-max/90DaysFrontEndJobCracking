/*
    1. = (assignment opeartor)
    2. == (comparison operator) (compare if values are equal)
    3. === strict equality operator (compare if values and datatype are equal)
    4. != strict inequality operator
    5. !== strict inequality operator

*/

// strict equality is quite useful when accepting input from the user
let pi = 3.14

if( pi == "3.14"){
    console.log("It is a pi");
} else {
    console.log("It is not a pi");
}