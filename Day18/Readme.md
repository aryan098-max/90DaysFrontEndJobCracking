1. Destructuring is a process of unpacking values into variables

    1. Array destrucutring
    2. Object destructuring

2. Use example of 'https://api.github.com/users/aryan098-max/repos'

    - Returns array of objects containing information about the repos
    - Extract firstRepo and seconRepo using array destructuring 
    - firstRepo and secondRepo is an obj, further destructure it using
      object destructuring
    - Now, since, the repo name has the same key as: name, use naming
      properties of object destructuring